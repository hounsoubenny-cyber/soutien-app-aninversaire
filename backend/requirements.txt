# === Framework et serveur ===
fastapi==0.122.0
uvicorn[standard]==0.38.0
python-multipart==0.0.21

# === Base de données (SQLite + PostgreSQL) ===
sqlmodel==0.0.31          # inclut SQLAlchemy 2.0.44
psycopg2-binary==2.9.11   # pour PostgreSQL (Supabase/Neon)

# === Sécurité ===
bcrypt==5.0.0
python-dotenv==1.0.1

# === IA (Groq) ===
groq==1.2.0

# === Rate limiting ===
slowapi==0.1.9

# === (optionnel) pour la compatibilité Pydantic v2 ===
pydantic==2.12.5          # déjà inclus par fastapi, mais on verrouille
sqlalchemy-libsql==0.2.0
